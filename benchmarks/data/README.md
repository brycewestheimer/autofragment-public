# Benchmark Data

Place large structure files here for profiling and benchmarks.

- `large_protein.pdb` is expected by `benchmarks/profile_partitioning.py`.
- Keep datasets out of version control if they are too large or restricted.
